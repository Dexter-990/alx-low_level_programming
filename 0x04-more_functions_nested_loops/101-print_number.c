#include "main.h"

/**
 * print_number - prints a number
 *
 * @n: number to be printed
 */

#include "main.h"

/**
 * print_number - prints a number
 *
 * @n: number to be printed
 */

void print_number(int n)
{
	if (n >= 0 && n <= 9)
	{
		_putchar(n + '0');
	}
	else if (
