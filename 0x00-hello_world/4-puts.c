#include <stdio.h>

/**
 * main - Prints Programming is like building a multilingual puzzle
 *
 * Description: Sets char as data type of str and makes it an array
 * section header: dajnca jnaslnlasnknas
 * Return: returns 0
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
